# Production overrides — supply these via environment variables
spring.datasource.url=${DATABASE_URL:jdbc:h2:mem:carloandb}
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=false
cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost}
logging.level.com.carloan=WARN
